// This file can be replaced during build by using the `fileReplacements` array.
// `ng build ---prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  // ApiUrl: "http://52.66.76.164:10000",
  // ApiUrl: "http://13.233.212.186:10000",
  //  ApiUrl: "http://13.233.212.186:10020",
  // ApiUrl: "https://api.ujustbe.com",
  //ApiUrl: "http://ec2-15-206-82-194.ap-south-1.compute.amazonaws.com",
  ApiUrl: "https://api-test.ujustbe.com",    
};

/*
 * In development mode, for easier debugging, you can ignore zone related error
 * stack frames such as `zone.run`/`zoneDelegate.invokeTask` by importing the
 * below file. Don't forget to comment it out in production mode
 * because it will have a performance impact when errors are thrown
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.

